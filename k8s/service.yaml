apiVersion: v1
kind: Service
metadata:
  name: happy-paws-frontend-service
  namespace: default
spec:
  type: NodePort
  selector:
    app: happy-paws-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 9090  # Match the containerPort defined in the deployment
      nodePort: 30007
